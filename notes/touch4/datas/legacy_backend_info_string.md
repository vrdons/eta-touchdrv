# legacy_backend_info_string

String literal at `.rodata:0x4a52b8`. Backend info row printed by [[print_usage]].

```c
char legacy_backend_info_string[] = "  legacy: Original geometric moment-based fitting (default)";
```
