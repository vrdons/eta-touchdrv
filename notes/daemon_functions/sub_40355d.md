- Init 2 local bytes:
- `var_9 = 0`
- `var_a = 0`

- Loop:
- `i` starts at `2`
- Continue while `i < *(arg1 + 0x28)`
- Each step reads `arg1[i]`

- Per iteration:
- Check if previous low 2 bits were zero: `var_9 == 0`
- Check if current low 2 bits non-zero: `(arg1[i] & 3) != 0`
- If both true, increment `var_a`

- After check:
- Store current low 2 bits into `var_9`:
- `var_9 = arg1[i] & 3`

- End:
- Return zero-extended `var_a`

- Effect:
- Counts transitions from state `0` to state `1..3` in low 2 bits of bytes from `arg1[2]` up to `arg1[*(arg1 + 0x28) - 1]`

```c
uint64_t sub_40355d(char* arg1)

{
    char var_9 = 0;
    char var_a = 0;
    
    for (int16_t i = 2; i < *(arg1 + 0x28); i += 1)
    {
        if (var_9 == 0 && (arg1[i] & 3) != 0)
            var_a += 1;
        
        var_9 = arg1[i] & 3;
    }
    
    return (uint64_t)(unsigned char)var_a;
}
```