# sub_401606

## Control flow

Debug symbol name: `packageBuild`. [[sub_40178b]] ve [[sub_40183d]] için 0x40 byte command packet oluşturur. Paket type, command id, index ve payload metadata alanlarını doldurur.

```c
void sub_401606(int type, unsigned char cmd, int index, int a3, unsigned char len, unsigned char *packet)
{
    ;
}
```
