# sub_40183d

## Control flow

Debug symbol name: `setCommand`. USB/HID feature write wrapper. [[sub_401606]] (`packageBuild`) ile type `1` paket kurar. Sonra fd üzerinden [[sub_401440]] (`ioctl`) request `0x100040` ile komutu gönderir.

Gönderim başarısızsa `0` döner. Başarılıysa `10000us` bekler, [[sub_4016d1]] (`deviceGetFeature`) ile command sonucunu okur, tekrar `10000us` bekler ve `1` döner.

Called by [[sub_405d74]]. Called functions: [[sub_401606]], [[sub_401440]], [[sub_4014f0]], [[sub_4016d1]].

```c
int sub_40183d(void *ctx, unsigned char cmd, int index, int a3, void *in, unsigned char in_len)
{
    unsigned char packet[0x40];

    sub_401606(1, cmd, index, a3, in_len, packet);
    if (sub_401440(*(int *)ctx, 0x100040, packet) < 0)
        return 0;

    sub_4014f0(10000);
    sub_4016d1(ctx, cmd, 0, 0);
    sub_4014f0(10000);
    return 1;
}
```
