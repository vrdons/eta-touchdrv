# sub_401781

## Control flow

Signal handler. Parametre olarak sinyal numarasını alır. Sinyal `15` (`SIGTERM`) veya `2` (`SIGINT`) ise global shutdown bayrağı [[data_4f5314]] değerini `1` yapar. Diğer sinyaller için işlem yapmaz.

Called by [[sub_40188e]]. Registered through [[sub_432c60]].

```c
void sub_401781(int sig)
{
    if (sig == 15 || sig == 2)
        data_4f5314 = 1;
}
```
