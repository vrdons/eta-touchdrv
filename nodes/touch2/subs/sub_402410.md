# sub_402410

## Control flow

Debug symbol name: `OptInterpolatorGetInsertCnt`. `interp == NULL` ise `0` döner. Null değilse [[sub_402210]] (`func_GetInsertLength`) sonucunu `*(float *)(interp+0x34)` ile çarpar, float sonucu signed int'e çevirir ve döndürür.

Called by [[sub_4058a9]]. Called functions: [[sub_402210]].

```c
int sub_402410(void *interp)
{
    float length;

    if (interp == 0)
        return 0;
    length = sub_402210(interp);
    return (int)(length * *(float *)((char *)interp + 0x34));
}
```
