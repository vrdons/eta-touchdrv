- Init:
- `var_9 = 0`
- `*arg3 = 0`
- `result_1 = 0`
- `var_e = 2`

- Loop:
- Read count/limit from `*(arg1 + 0x28)`
- Stop when `var_e u>= *(arg1 + 0x28)`

- Per entry `arg1[var_e]`:
- If current state `var_9 == 0` and low 2 bits of `arg1[var_e]` nonzero:
  - increment `*arg3`
  - store current `result_1` into start field of current record at `arg2`
- Else if current state `var_9 != 0` and low 2 bits of `arg1[var_e]` zero:
  - store current `result_1` into end field of current record at `arg2 + 2`

- After state check:
- advance `result_1` by `((arg1[var_e] u>> 2) + 1)`
- update `var_9` to low 2 bits of `arg1[var_e]`
- increment `var_e`

- After loop:
- If final state `var_9 != 0`, close last open record by storing `result_1` into end field of current record

- Return:
- returns last loaded loop bound from `*(arg1 + 0x28)`, or `result_1` in final-open case before same value overwritten by store path in decomp view. Effective purpose looks like interval extraction, not return value use.

## Cleaned C code

```c
int64_t sub_4035d0(char* arg1, int64_t arg2, char* arg3)

{
    char var_9 = 0;
    *arg3 = 0;
    int16_t result_1 = 0;
    int16_t var_e = 2;
    int16_t result;
    
    while (true)
    {
        result = *(arg1 + 0x28);
        
        if (var_e u>= result)
            break;
        
        if (var_9 == 0 && (zx.d(arg1[sx.q(zx.d(var_e))]) & 3) != 0)
        {
            *arg3 += 1;
            *((zx.q(*arg3) << 2) - 4 + arg2) = result_1;
        }
        else if (var_9 != 0 && (zx.d(arg1[sx.q(zx.d(var_e))]) & 3) == 0)
            *((zx.q(*arg3) << 2) - 4 + arg2 + 2) = result_1;
        
        result_1 += zx.w((arg1[sx.q(zx.d(var_e))] u>> 2) + 1);
        var_9 = arg1[sx.q(zx.d(var_e))] & 3;
        var_e += 1;
    }
    
    if (var_9 != 0)
    {
        result = result_1;
        *((zx.q(*arg3) << 2) - 4 + arg2 + 2) = result;
    }
    
    return result;
}
```