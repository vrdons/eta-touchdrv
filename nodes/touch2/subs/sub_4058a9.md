# sub_4058a9

## Control flow

Debug symbol name: `DeviceProcess`. Parametreler device process context, raw USB buffer, buffer length. Packet header `buf[2] == 1` değilse hemen döner. `buf[4] == 0` ise gelen chunk context offset `+0x8` altına kopyalanır ve uzunluk `+0x48` alanına yazılır.

`buf[4] != 0` ise önce önceki chunk var mı diye `ctx+0x48` kontrol edilir. Yoksa döner. Varsa eski chunk ve yeni buffer [[sub_401674]] (`decode`) ile iki code plane'e decode edilir. Context bayrağına göre [[sub_405415]] (`OptProcessCode`) çağrısı iki farklı sıra ile yapılır.

Sonra [[sub_4057c9]] (`OptGetOutputPoints`) raw output points üretir. Points [[sub_401d75]] (`OptClickFilterSetPoint`) ve [[sub_401e14]] (`OptInterpolatorSetPoint`) zincirinden geçer. [[sub_401a6f]] ve [[sub_4021e4]] ile filtrelenmiş/interpole edilmiş point listeleri alınır.

Her point için [[sub_4055d4]] (`OptTranslatePoint`) ile ekran koordinatına çeviri yapılır. X/Y değerleri `0..0x7fff` aralığına kırpılır. Sonuç 9-byte HID-like report alanlarına paketlenir. Son aşamada fd üzerinden [[sub_401440]] (`ioctl`) ile `0x220014` request gönderilir. Context son point state alanları güncellenir.

Called functions: [[sub_401674]], [[sub_401430]], [[sub_401440]], [[sub_401a6f]], [[sub_401d75]], [[sub_401e14]], [[sub_4021e4]], [[sub_402410]], [[sub_405415]], [[sub_4055d4]], [[sub_4057c9]].

```c
void sub_4058a9(void *ctx, unsigned char *buf, unsigned char len)
{
    if (buf[2] != 1)
        return;

    if (buf[4] == 0) {
        if (len != 0)
            memcpy((char *)ctx + 8, buf, len);
        *(unsigned char *)((char *)ctx + 0x48) = len;
        return;
    }

    if (*(unsigned char *)((char *)ctx + 0x48) == 0)
        return;

    sub_401674((char *)ctx + 8, plane0);
    sub_401674(buf, plane1);

    if (*(unsigned char *)((char *)ctx + 0x64))
        sub_405415(plane0, 0x280, plane1, 0x280, (char *)ctx + 0x65);
    else
        sub_405415(plane1, 0x280, plane0, 0x280, (char *)ctx + 0x65);

    sub_4057c9(raw_points, out_count, (char *)ctx + 0x65);
    sub_401d75(raw_points, (char *)ctx + 0xf3f);
    sub_401e14(out_count, (char *)ctx + 0x1265);
    sub_401a6f(filtered, &count, (char *)ctx + 0xf3f);
    sub_4021e4(interp_points, &count, (char *)ctx + 0x1265);

    for (int i = 0; i < count; i++) {
        sub_401e14(&filtered[i], (char *)ctx + 0x129d);
        sub_401e14(&interp_points[i], (char *)ctx + 0x12d5);
        int insert_count = sub_402410((char *)ctx + 0x129d);
        sub_4021e4(inserted0, &insert_count, (char *)ctx + 0x129d);
        sub_4021e4(inserted1, &insert_count, (char *)ctx + 0x12d5);
        for (int j = 0; j < insert_count; j++) {
            struct point p;
            sub_4055d4(raw_value, &p, &inserted0[j], (char *)ctx + 0x65, &inserted1[j]);
            if (p.x < 0) p.x = 0;
            if (p.x > 0x7fff) p.x = 0x7fff;
            if (p.y < 0) p.y = 0;
            if (p.y > 0x7fff) p.y = 0x7fff;
        }
        sub_401440(*(int **)(ctx)[0], 0x220014, report);
    }
}
```
