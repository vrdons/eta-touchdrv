- Fonksiyon herhangi bir parametre almaz.
- 128-bitlik zmm0 ile zmm7 arasındaki vektör/SIMD register'larını tanımlar.
- Bu register'ları verilerle doldurmak için [[sub_41ef20]] fonksiyonunu çağırır.
- [[sub_41ef20]] tarafından doldurulan bu 8 adet register verisini alıp, asıl işlemin yapılacağı [[sub_42029f]] fonksiyonuna argüman olarak gönderir.
- [[sub_42029f]] fonksiyonunun ürettiği sonucu direkt olarak döndürür.

```c
uint64_t sub_401770() {
    uint128_t zmm0;
    uint128_t zmm1;
    uint128_t zmm2;
    int128_t zmm3;
    int128_t zmm4;
    int128_t zmm5;
    int128_t zmm6;
    int128_t zmm7;
    
    zmm0, zmm1, zmm2, zmm3, zmm4, zmm5, zmm6, zmm7 = sub_41ef20();
    
    return sub_42029f(zmm0, zmm1, zmm2, zmm3, zmm4, zmm5, zmm6, zmm7);
}
```