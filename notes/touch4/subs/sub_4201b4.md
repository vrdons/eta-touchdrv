# sub_4201b4

## Control flow

Debug/log çevre değerini global state içine uygular. [[sub_40188e]] içinde `-e` değeri veya [[data_4a53d0]] (`TOUCH_DEBUG_LOG`) çevre değişkeninden gelen değerle çağrılır. Eski global pointer [[data_4f5328]] değerini alır; parametre `NULL` ise globali sıfırlar, değilse yeni değeri kaydeder ve gerekirse önceki değeri temizler.

Called by [[sub_40188e]].

```c
void sub_4201b4(char *value)
{
    char *old = data_4f5328;

    if (value == 0) {
        data_4f5328 = 0;
        if (old == 0)
            return;
    } else {
        ;
    }
}
```
