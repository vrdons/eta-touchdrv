- Function takes two pointers to `int16_t`.
- First compare `*arg2` with `arg1[1]`.
- If `*arg2 u>= arg1[1]`, return unsigned difference:
  $*arg2 - arg1[1]$
- Else compare `*arg1` with `arg2[1]`.
- If `*arg1 u< arg2[1]`, return `0`.
- Else return unsigned difference:
  $*arg1 - arg2[1]$

- Function checks gap/overlap between two 2-element ranges.
- `arg1[0]` and `arg1[1]` act like start/end for first range.
- `arg2[0]` and `arg2[1]` act like start/end for second range.
- Return value:
  - positive distance if second range starts after first range ends
  - `0` if ranges overlap or touch
  - positive distance if first range starts after second range ends

```c
uint64_t sub_4034f8(int16_t* arg1, int16_t* arg2)

{
    if (*arg2 >= arg1[1])
        return (uint32_t)(*arg2 - arg1[1]);
    
    if (*arg1 < arg2[1])
        return 0;
    
    return (uint32_t)(*arg1 - arg2[1]);
}
```