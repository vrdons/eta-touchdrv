# sub_40178b

## Control flow

Debug symbol name: `getCommand`. USB/HID feature read wrapper. Önce stack üzerinde 0x40 byte paket üretmek için [[sub_401606]] (`packageBuild`) çağrılır; type parametresi `2` yani get command. Paket fd üzerinden [[sub_401440]] (`ioctl`) ile `0x100040` request olarak gönderilir.

İlk ioctl başarısızsa `0` döner. Başarılıysa [[sub_4014f0]] ile `10000us` bekler, sonra [[sub_4016d1]] (`deviceGetFeature`) çağırarak cevap bufferını doldurur. Bu da başarısızsa `0`; başarılıysa tekrar `10000us` bekleyip `1` döner.

Called by [[sub_405d74]]. Called functions: [[sub_401606]], [[sub_401440]], [[sub_4014f0]], [[sub_4016d1]].

```c
int sub_40178b(void *ctx, unsigned char cmd, int index, int a3, unsigned char flag, void *out, int out_len)
{
    unsigned char packet[0x40];

    sub_401606(2, cmd, index, a3, flag, packet);
    if (sub_401440(*(int *)ctx, 0x100040, packet) < 0)
        return 0;

    sub_4014f0(10000);

    if (sub_4016d1(ctx, cmd, out, out_len) < 0)
        return 0;

    sub_4014f0(10000);
    return 1;
}
```
