# touch_debug_log_env_name

Environment variable name for debug log configuration.

Value: `TOUCH_DEBUG_LOG`

Referenced by [[main]] and read through `getenv`.

```c
char touch_debug_log_env_name[] = "TOUCH_DEBUG_LOG";
```
