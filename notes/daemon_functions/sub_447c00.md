- `arg1 == 0` ise hemen döner.
- `rsI = arg1 - 0x10` alır.
- `rax = *(arg1 - 8)` ile metadata/flag değer okur.
- `rbp = zx.q(*(fsbase - 0x48))` ile thread-local alan saklanır.
- `(rax.b & 2) != 0` ise özel yol:
  - [[data_4f37a8]]` == 0` ve [[data_4f3770]]` < rax <= 0x2000000` ise:
    - `rax & 0xfffffffffffffff8` hesaplanır.
    - sonuç [[data_4f3770]] içine yazılır.
    - iki katı [[data_4f3760]] içine yazılır.
  - sonra [[sub_443670]] çağrılır.
  - `*(fsbase - 0x48) = rbp.d` ile thread-local alan geri yüklenir.
  - döner.
- `(rax.b & 2) == 0` ise devam eder.
- `*(fsbase - 0x40) == 0 && *(fsbase - 0x38) == 0` ise:
  - SIMD argümanlar [[sub_446f00]] içinden geçirilir.
  - sonra `rax = *(arg1 - 8)` tekrar okunur.
- Varsayılan `rdI = &[[data_4f3800]]`.
- `(rax.b & 4) != 0` ise:
  - `rdx_1 = [[data_4f3790]]`
  - `rax_2 = -(rdx_1 << 2)`
  - `rdx_1 == 0` ise `rax_2 = -0x4000000`
  - `rdI = *(rax_2 & rsI)` olur.
- Sonunda [[sub_444d90]] çağrılır:
  - `rdI`
  - `rsI`
  - sabit `0`
  - tüm vektör argümanlar
  - sabit `-0xffffffffffffffb8`
  - saklanan `rbp`
  - ek register argümanları
- Son olarak `*(fsbase - 0x48) = rbp.d` ile thread-local alan geri yüklenir.

```c
void sub_447c00(void* arg1, uint128_t arg2, uint128_t arg3, uint128_t arg4, int128_t arg5, int128_t arg6, int128_t arg7, int128_t arg8, int128_t arg9)
{
    if (arg1 == 0)
        return;
    
    int64_t* rsI = arg1 - 0x10;
    int64_t rax = *(arg1 - 8);
    void* fsbase;
    uint64_t rbp = zx.q(*(fsbase - 0x48));
    
    if ((rax.b & 2) != 0)
    {
        if (data_4f37a8 == 0 && data_4f3770 u< rax && rax u<= 0x2000000)
        {
            int64_t rax_5 = rax & 0xfffffffffffffff8;
            data_4f3770 = rax_5;
            data_4f3760 = rax_5 * 2;
        }
        
        sub_443670(rsI, arg2, arg3, arg4, arg5, arg6, arg7, arg8, arg9);
        *(fsbase - 0x48) = rbp.d;
        return;
    }
    
    if (*(fsbase - 0x40) == 0 && *(fsbase - 0x38) == 0)
    {
        arg2, arg3, arg4, arg5, arg6, arg7, arg8, arg9 =
            sub_446f00(arg2, arg3, arg4, arg5, arg6, arg7, arg8, arg9);
        rax = *(arg1 - 8);
    }
    
    int32_t* rdI = &data_4f3800;
    
    if ((rax.b & 4) != 0)
    {
        int64_t rdx_1 = data_4f3790;
        int64_t rax_2 = neg.q(rdx_1 << 2);
        
        if (rdx_1 == 0)
            rax_2 = -0x4000000;
        
        rdI = *(rax_2 & rsI);
    }
    
    int64_t* r14;
    int64_t r15;
    sub_444d90(rdI, rsI, 0, arg2, arg3, arg4, arg5, arg6, arg7, arg8, arg9, -0xffffffffffffffb8, rbp, r14, r15);
    *(fsbase - 0x48) = rbp.d;
}
```