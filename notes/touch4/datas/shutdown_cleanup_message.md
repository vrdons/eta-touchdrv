# shutdown_cleanup_message

String literal at `.rodata:0x4a5410`. Shutdown cleanup message printed by [[main]].

```c
char shutdown_cleanup_message[] = "Shutdown requested, cleaning up" "." "." ".";
```
