- Function initializes output buffer at `arg5`.
- Sets:
  - `arg5[0] = 0`
  - `arg5[1] = 0`
  - `*(arg5 + 0x28) = 2`
- Local state tracks two interval streams:
  - first stream from `arg1`, count `arg2`
  - second stream from `arg3`, count `arg4`
- Each stream uses:
  - current boundary position
  - current inside/outside state
  - current index
- Main loop runs while `i u<= 0x280`.

- On each iteration:
  - Compute current combined state from `var_18` and `var_38`.
  - If combined state changed from previous state `var_58`, flush accumulated run length `j` into `arg5`.
  - Encoding uses bytes written at offset `*(arg5 + 0x28)`.
  - If `j u<= 0x40`, emit one byte:
    - `((j - 1) << 2) + state_bit`
  - If `j > 0x40`, emit continuation byte:
    - `-4` when previous state was `0`
    - `-3` when previous state was `1`
    - then subtract `0x40` and continue
- Stop flushing if output position exceeds `0x27`.

- Next event position chosen from smaller of `j_1` and `j_2`.
- Accumulated run length `j` increases by distance from current position `i` to next event.
- Then:
  - if `j_1 u< j_2`, advance `i` to `j_1` and update state from second stream
  - else advance `i` to `j_2` and update state from first stream

- After advancing:
  - chosen stream updates next boundary/state
  - if currently inside interval, load interval end and skip nested/overlapping ranges
  - if currently outside interval, load next interval start
  - if stream exhausted, set boundary to `0x281`

- Loop continues until position passes `0x280`.

- Final result:
  - return encoded length from `*(arg5 + 0x28)`
  - if no payload bytes emitted and length still `2`, clear `*(arg5 + 0x28)` to `0` and return `arg5`

- Function merges two interval boundary lists and emits run-length style encoded state transitions into output buffer.
- Encoded state bit depends on whether either stream currently active.
- Stream entries appear as 2-element ranges stored as 4-byte records:
  - start at offset `+0`
  - end at offset `+2`

```c
uint64_t sub_403701(int64_t arg1, char arg2, int64_t arg3, char arg4, char* arg5)

{
    int16_t j_2 = 0;
    char var_26 = 0;
    int64_t var_20 = 0;
    int64_t var_18 = 0;
    int16_t j_1 = 0;
    char var_46 = 0;
    int64_t var_40 = 0;
    int64_t var_38 = 0;
    int16_t i = 0;
    char var_66 = 0;
    int64_t var_60 = 0;
    int64_t var_58 = 0;
    arg5[1] = 0;
    *arg5 = arg5[1];
    *(arg5 + 0x28) = 2;
    int16_t j = 0;
    
    while (i <= 0x280)
    {
        int32_t rax_7;
        
        rax_7 = var_18 != 0 || var_38 != 0 ? 1 : 0;
        
        int64_t rax_8 = (int64_t)rax_7;
        
        if (rax_8 != var_58)
        {
            while (j != 0)
            {
                if (*(arg5 + 0x28) > 0x27)
                    break;
                
                if (j <= 0x40)
                {
                    char rcx_1;
                    
                    rcx_1 = var_58 == 0 ? 0 : 1;
                    
                    arg5[(uint32_t)*(arg5 + 0x28)] =
                        rcx_1 + ((uint8_t)(j - 1) << 2);
                    j = 0;
                    *(arg5 + 0x28) += 1;
                }
                else
                {
                    char rcx;
                    
                    rcx = var_58 == 0 ? -4 : -3;
                    
                    arg5[(uint32_t)*(arg5 + 0x28)] = rcx;
                    j -= 0x40;
                    *(arg5 + 0x28) += 1;
                }
            }
            
            var_58 = rax_8;
        }
        
        if (j_1 < j_2)
        {
            j += j_1 - i;
            i = j_1;
            var_38 = var_40;
        }
        else
        {
            j += j_2 - i;
            i = j_2;
            var_18 = var_20;
        }
        
        if (j_1 < j_2)
        {
            if (var_40 != 0)
            {
                do
                {
                    j_1 = *(arg3 + ((uint32_t)var_46 << 2) + 2);
                    var_46 += 1;
                    
                    if (var_46 >= arg4)
                        break;
                } while (j_1 >= *(arg3 + ((uint32_t)var_46 << 2)));
                
                var_40 = 0;
            }
            else if (var_46 >= arg4)
            {
                j_1 = 0x281;
                var_40 = 0;
            }
            else
            {
                j_1 = *(arg3 + ((uint32_t)var_46 << 2));
                var_40 = 1;
            }
        }
        else if (var_20 != 0)
        {
            do
            {
                j_2 = *(arg1 + ((uint32_t)var_26 << 2) + 2);
                var_26 += 1;
                
                if (var_26 >= arg2)
                    break;
            } while (j_2 >= *(arg1 + ((uint32_t)var_26 << 2)));
            
            var_20 = 0;
        }
        else if (var_26 >= arg2)
        {
            j_2 = 0x281;
            var_20 = 0;
        }
        else
        {
            j_2 = *(arg1 + ((uint32_t)var_26 << 2));
            var_20 = 1;
        }
    }
    
    uint64_t result = (uint32_t)*(arg5 + 0x28);
    
    if ((uint32_t)result == 2)
    {
        result = (uint64_t)arg5;
        *(result + 0x28) = 0;
    }
    
    return result;
}
```