# verbose_continuation_help_row

String literal at `.rodata:0x4a51e0`. Continuation help row printed by [[print_usage]].

```c
char verbose_continuation_help_row[] = "                                    verbosity (-v, -vv, -vvv, " "." "." "." ")";
```
