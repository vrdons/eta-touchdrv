# sub_41d272

## Control flow

Large descriptor/table initializer. If destination or table pointer is null, returns `0`. It repeatedly calls lower parser helper with fixed offsets and sizes, writing callback/function pointers into destination when parsed blocks exist and clearing them when absent. The first block fills `dst+0x10` size `0x19` and sets `dst[0]` to handler `0x41bec9` if present. The next block fills `dst+0x40` size `0x22` and sets `dst+0x30` to handler `0x41bf6c` if present. Function continues same descriptor pattern through whole structure and returns destination on success, `0` on required-block failure.

Called by [[sub_41e9fa]].

```c
void *sub_41d272(void *dst, void *table, void *ctx)
{
    if (dst == 0 || table == 0)
        return 0;

    if (sub_41bcb0(table, 0, ctx, (char *)dst + 0x10, 0x19) != 0)
        *(void **)dst = (void *)0x41bec9;
    else
        *(void **)dst = 0;

    if (sub_41bcb0(table, 0x0c, ctx, (char *)dst + 0x40, 0x22) != 0)
        *(void **)((char *)dst + 0x30) = (void *)0x41bf6c;
    else
        *(void **)((char *)dst + 0x30) = 0;

    return dst;
}
```
