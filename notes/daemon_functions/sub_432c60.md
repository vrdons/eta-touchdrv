# sub_432c60
- Kontrol akışı
  - Başta stack canary okunur: `*(fsbase + 0x28)`.
  - Şu koşullardan biri doğruysa hata:
    - `arg1 - 1 u> 0x3f`  -> `arg1` aralık dışı
    - `arg2 == -1`
    - `arg1 - 0x20 u<= 1` -> `arg1 == 0x20` veya `arg1 == 0x21`
  - Hata durumunda:
    - `*(fsbase - 0x48) = 0x16`
    - dönüş değeri `-1`
  - Geçerli durumda:
    - `arg2` yerel değişkene kopyalanır.
    - `1 << ((arg1 - 1).b)` hesaplanır.
    - sonuç [[data_4f5d40]] ile `AND` edilir.
    - karşılaştırma sonucu tek bayrak değeri üretir ve `var_c0_1` içine `0` veya `0x10000000` olarak yazılır.
    - sonra [[sub_432d60]] çağrılır:
      - `arg1`
      - `&var_148`
      - `&result_1`
      - SIMD argümanları
    - [[sub_432d60]] dönüş değeri negatifse son dönüş değeri `-1` yapılır.
    - değilse `result_1` döndürülür.
  - Sonda stack canary doğrulanır.
  - Canary bozuksa [[sub_466a60]] çağrılır ve fonksiyon dönmez.

```c
int64_t sub_432c60(
    int32_t arg1,
    int64_t arg2,
    int128_t arg3,
    int128_t arg4,
    int128_t arg5,
    int128_t arg6,
    int128_t arg7,
    int128_t arg8,
    int128_t arg9,
    int128_t arg10)
{
    int64_t canary = *(int64_t *)(fsbase + 0x28);
    int64_t result;

    if ((uint32_t)(arg1 - 1) > 0x3f || arg2 == -1 || (uint32_t)(arg1 - 0x20) <= 1) {
        *(int32_t *)(fsbase - 0x48) = 0x16;
        result = -1;
    } else {
        int64_t value = arg2;
        int64_t mask = 1LL << (uint8_t)(arg1 - 1);
        uint32_t var_c0_1 = ((mask & data_4f5d40) == 0) ? 0x10000000 : 0;
        int64_t result_1;
        int32_t rc = sub_432d60(
            arg1, &value, &result_1,
            arg3, arg4, arg5, arg6, arg7, arg8, arg9, arg10
        );

        result = result_1;
        if (rc < 0)
            result = -1;
    }

    if (canary == *(int64_t *)(fsbase + 0x28))
        return result;

    sub_466a60(arg3, arg4, arg5, arg6, arg7, arg8, arg9, arg10);
}
```